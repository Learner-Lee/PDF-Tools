# PDF Tools — 设计文档

> 本文档是需求讨论的结论固化，实现以此为准。最后更新：2026-08-31

## 0. 一句话

一个本地运行的 Web 应用，把英文 PDF 变成可读的中文内容，提供三种阅读/产出模式：**左右对照实时翻译**、**保留版式的全文翻译导出**、**只标注难词的原文阅读**。

第一版只支持**文字版 PDF**（含 text layer）。扫描件/图片 PDF 检测后给出明确提示，OCR 留到第二期。

---

## 1. 功能定义

### 1.1 对照阅读（MVP，优先做）

- 左栏：pdf.js 渲染**真实 PDF 页面**，保留图表与原始排版
- 右栏：纯文本中文译文，按**文本块**与左侧对齐
- **双向滚动同步**：左栏滚动 → 右栏定位到对应块；点击右栏某段 → 左栏高亮对应 bbox
- **懒加载翻译**：只翻译当前可见页 + 预取后 2 页；结果流式返回并写入缓存
- 顶部提供「翻译全文」按钮，后台批量补齐剩余页面

### 1.2 全文翻译导出

- 产出**保留原版式的中文 PDF**（主产物）
- 同时**无条件产出一份 Markdown 保底**：版面重建失败或结构过于复杂时，用户仍有可读内容
- 与对照阅读**共用同一份翻译缓存**：已经看过的页面不重复调用 API

### 1.3 难词模式

- 整篇**保持英文原文**，只对超出用户词汇量的词做标注
- **入口是对照视图里的一个可切换图层**，不是独立页面：同一个阅读器，顶部切换「对照翻译 / 难词标注」，解析结果与滚动位置共用
- 交互：难词加下划线/底色 → **hover 弹出释义卡片**（中文释义、音标、词性、本文中的例句）
- 卡片上有收藏按钮 → 累积成**生词本** → 导出 **Anki / CSV**
- 难度基准**两套都支持，用户自选**：
  - COCA 词频档位（前 3000 / 5000 / 8000 / 15000）
  - 考试词表（CET4 / CET6 / IELTS / TOEFL / GRE）
- 无论哪套，**AWL 学术词表反向豁免**：读论文的人本就熟悉这些词，即使低频也不标注（可在设置中关闭）

---

## 2. 版面策略：结构化局部回流

导出「保留原版式的中文 PDF」时采用的原则：

**保留不变**
- 页面尺寸、栏数（单栏/双栏）
- 图片、图表、矢量图形的位置与尺寸
- 标题层级、列表与表格结构
- 字体样式的**相对关系**（粗体、斜体、字号层级）

**允许变化**
- 单个文本块的高度：译文长了向下推挤后续块，图片作为锚点绕开
- **总页数允许变化**，文本自然溢到下一页 —— 绝不重叠、绝不丢内容
- 实践上中译文排版长度约为英文的 0.6–0.8 倍，页数往往持平或减少

**中间层**
翻译不直接在 PDF 上做，而是先解析成**结构化文档模型**（块类型 + bbox + 样式 + 阅读顺序），翻译后按模型重新渲染。
> 注：不用 Markdown 做中间格式 —— 它表达不了双栏和精确图片位置，会丢信息。Markdown 是并行的保底产物，不是渲染管线的一环。

---

## 3. 架构

```
上传 PDF
   ↓
[解析层] PyMuPDF → 文档模型 (DocModel JSON) ─────→ SQLite 缓存
   ↓
[翻译层] Provider 抽象 → Qwen API / llama.cpp ──→ SQLite 缓存（按块）
   ↓                                                   ↓
[对照视图]                                        [渲染层] 回流重排 → 中文 PDF
   pdf.js + 译文栏                                             └→ Markdown 保底
```

### 3.1 解析层

1. **文字版检测**：`page.get_text()` 字符数低于阈值 → 判定为扫描件，提示暂不支持
2. **块提取**：`page.get_text("dict")` 拿到 block / line / span，带 bbox、字体、字号、颜色、粗斜体标志
3. **栏检测**：文本块 x 中心聚类 + 垂直空白带投影 → 判定单栏 / 双栏
4. **阅读顺序重建**：单栏按 y 排序；双栏先分栏、栏内按 y、左栏走完再右栏
5. **块类型分类**
   - 标题：字号 > 正文中位字号 × 1.15，或整块 bold
   - 页眉页脚：y 位于页面上下 8% 且跨页重复
   - **公式**：span 使用数学字体（CMMI / CMSY / MSAM / STIX 等）或符号密度超阈值 → **标记为不翻译**
   - 代码块：等宽字体 → **不翻译**
   - 参考文献：`References` / `Bibliography` 标题之后 → **默认不翻译**（已确认；设置中可开启）
   - 图注 / 表格单元格 / 正文
6. **段落合并**：跨行、跨栏、跨页的同一段落合并为一个翻译单元（行尾无句末标点 + 下行首字母小写 → 判为续行）

### 3.2 翻译层

统一 Provider 接口，两个实现走**同一套 OpenAI 兼容调用代码**，切换只改 base_url：

| Provider | Base URL | 模型 |
|---|---|---|
| `QwenProvider`（默认） | `https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1` | 翻译与释义均用 `qwen3.6-flash` |
| `LlamaCppProvider` | `http://localhost:8080/v1` | llama.cpp server |

**模型选型**：套餐内不含翻译专用模型（无 `qwen-mt-*`），按「便宜够用」选 `qwen3.6-flash`（套餐内最便宜的文本模型）。质量不满意时改 `.env` 一行切到 `qwen3.7-plus`，代码无需改动。

#### ⚠️ 必须关闭思考模式

`qwen3.x` 系列默认是推理模型，会输出 `reasoning_content`。实测翻译单句：

| 配置 | completion_tokens |
|---|---|
| 默认（带思考） | 300+ |
| `enable_thinking: false` | **7** |

批量翻译整本 PDF 时差异约 **40 倍成本**。所有请求**必须**携带 `enable_thinking: false`，这是 provider 层的硬性默认值，不做成可选项。

#### 批量分段协议（已实测通过）

一次请求翻译多个相邻段落，用编号 + JSON 数组回收，保证段落对齐不错位：

```
system: 你是专业的学术翻译。将用户提供的编号英文段落逐条翻译成中文。
        严格按 JSON 数组输出，每个元素形如 {"id":<编号>,"zh":"<译文>"}，不要输出任何其他内容。
        保留术语一致性，保留原文中的数字、公式、引用标记。
user:   [0] Recent advances in transformer architectures...
        [1] However, the computational cost remains...
```

- `temperature: 0.3`，解析时剥离可能的 ```json 代码围栏
- 返回条数与请求条数不符时，降级为逐条单独翻译（保证不错位）

- **批处理**：相邻段落打包成单个请求（约 2000 token 一批），附带上文片段维持语境
- **术语一致性**：先扫全文抽取高频专有名词/缩写，生成 glossary 注入 prompt，保证全篇译名统一
- **缓存 key**：`sha256(文件) + block_id + 目标语言 + provider + model` —— 换引擎不会误用旧结果

### 3.3 渲染层（原版式导出）

1. 以原页为底，`add_redact_annot` + `apply_redactions` 抹掉文字层，**图片与矢量图完整保留**
2. 嵌入开源中文字体（思源黑体 / 思源宋体），按原块的样式层级映射字重
3. 按阅读顺序回填译文：维护 per-column 的 y 游标，块高不足则向下推挤；遇图片锚点跳过其占用区域；超出页面下沿则开新页
4. 字号策略：优先沿用原字号，塞不下时最多缩至 85%，仍不够则回流到下一页（不重叠优先于保页数）

### 3.4 难词判定

1. **词形还原**（simplemma，轻量无模型依赖）：`mitigated` → `mitigate`
2. **难度判定**：lemma 的 COCA 词频排名超过用户阈值（或不在所选考试词表内）
3. **排除**：停用词、专有名词（首字母大写且非句首）、数字、已收藏为「已掌握」的词、AWL 豁免词
4. **释义生成**：批量提交 `难词 + 所在完整句子` → `qwen-plus` → 返回词性 + 语境中文释义
5. **音标**：从 CMU Pronouncing Dictionary 离线查询，**不让 LLM 生成**（避免幻觉）

---

## 4. 技术栈

- **后端**：Python + FastAPI，PyMuPDF 解析与重排
- **前端**：React + pdf.js
- **存储**：SQLite（文档模型、翻译缓存、生词本、设置）
- **部署**：本地运行，浏览器访问，无需登录

### 目录结构

```
pdf-tools/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── api/           # upload / document / translate / export / vocab / settings
│   │   ├── parser/        # extract, layout, classify, model
│   │   ├── translator/    # base, qwen, llamacpp, glossary, cache
│   │   ├── renderer/      # reflow, pdf_writer, markdown_writer
│   │   ├── vocab/         # lemmatize, difficulty, gloss
│   │   └── db/
│   └── requirements.txt
├── frontend/
│   └── src/
│       ├── components/    # PdfPane, TransPane, HardWordLayer, VocabBook, Settings
│       ├── hooks/         # useSyncScroll, useLazyTranslate
│       └── api/
├── data/
│   ├── wordlists/         # coca_60k, cet4, cet6, ielts, toefl, gre, awl
│   └── fonts/             # SourceHanSansSC, SourceHanSerifSC
└── storage/               # uploads/, exports/, cache.db
```

---

## 5. 实施顺序

**按功能**（MVP 先打通对照阅读 —— 解析层与翻译层是后两个功能的共同地基）

1. 上传 → 解析 → 文档模型 → 左右对照 → 懒加载翻译 → 缓存
2. 难词模式（复用解析层，成本最低）
3. 全文翻译导出（原版式重建，最重）

**按版面难度**

1. **单栏文档**：技术文档 / 电子书 / 报告 / 说明书 —— 阅读顺序基本自上而下
2. **双栏学术论文**：需额外做栏检测与跨栏元素（宽表、跨栏公式）处理
3. **演示文稿 / 合同**：版面自由度最高，最后处理

---

## 6. 明确的非目标（第一版）

- 扫描件 / 图片 PDF 的 OCR
- 多用户、登录、云端部署
- 中译英等反向翻译
- PDF 编辑、合并、拆分等通用工具（"工具集合"的其余部分留待后续扩展）

---

## 附录 A：真实样本验证结论

样本：`2605.01017v2-8.pdf`（arXiv / ACL 模板，19 页双栏英文论文，含中文示例）

解析结果：19 页全部正确识别为双栏，414 个块，117 个待翻译（37,809 字符），无归宿块 0 个。

**以下四条原设计规则被真实数据推翻，已修正：**

| 原规则 | 实际情况 | 修正后 |
|---|---|---|
| 标题 = 字号 > 正文 × 1.15 | `4.4 Corpus analysis` 字号 10.9 **小于**正文 11.0 | 改用字重（`flags & 16`），字号不参与判定 |
| CMR10 属数学字体，不翻译 | CMR10 排的是编号列表，排除会丢整段 | 数学只认 `CMMI/CMSY/CMEX/MSAM/EU*` |
| 栏检测按跨栏块**数量**占比 | 论文首页有 5 个跨栏块（标题/作者/单位/邮箱），首页被误判单栏，右栏内容排到左栏前 | 改按跨栏块**文本量**占比 |
| 行尾 `-` + 下行小写 = 断词 | 复合词的连字符恰好落在行尾时被误删（`part-ofspeech`、`readergrounded`） | 见下 |

**连字符消歧**（软连字符 vs 复合词，PDF 中无法直接区分）：
1. 全文扫描行**内**的连字符复合词建词表；候选整体命中则保留连字符
2. 兜底：断开两半若各自都是文档中的独立词（`mass`|`produce`）→ 复合词；两半都不成词（`classi`|`fiers`）→ 断词
3. 该判定块内换行与跨块换行共用一份实现 —— 实测 `part-of-` 与 `speech,` 分属两个块

**其他实测发现：**
- **arXiv 侧边竖排水印**（x<40、高 350pt）必须过滤，否则混入正文
- **中文逐字成 span**，字间距天然超空格阈值，不加防护会得到 `AI最 新 排 行 榜`
- **31 个块本身就是中文**（论文的中文 prompt 与小红书示例），必须跳过翻译，否则二次翻译产出垃圾
- 段落跨块/跨栏/跨页拆分普遍存在：本文 16 处，合并了 18 个块

---

## 附录 B：翻译层实测结论

样本同上，116 个待翻译块 / 37,658 字符，全部成功，0 失败。

### 成本

| 项 | 实测 |
|---|---|
| 单次干净全量 | 约 3.5 万 token（含术语表调用） |
| API 调用次数 | 21 次（批处理，均值 5.5 块/次） |
| 耗时 | 19 秒 |
| 缓存复用 | 二次运行 116 块全部命中，0 次调用 |

参考文献（258 块）与中文块（31 块）不翻译，是成本能压到这个量级的主因。

### 模型不守格式，解析必须容错

实测同一个 prompt 会得到三种形态，起初有 46/116 块因此失败：

| 模型实际输出 | 说明 |
|---|---|
| `[{"id":0,"zh":"..."}]` | 规范格式 |
| `{"id":0,"zh":"..."}` | 裸对象，单段时尤其常见 |
| `[0] 2 相关工作` | 直接模仿输入的编号格式 |

更麻烦的是第三种：正则兜底 `\[.*\]` 会把 `[0]` 匹配成合法 JSON 数组，
解析出 `[0]` 后在整数上取 `["id"]` 崩溃 —— **容错逻辑反而吃掉了本可用的结果**。

现在 `parse_segments()` 依次尝试：整体 JSON（数组或裸对象）→ 逐行 JSONL →
`[N] 译文` 编号格式 → 单段时整段即译文。

**单块降级路径不再要求 JSON**，直接收纯文本。与其和模型较劲格式，不如换个要求。

### 其他

- 批量返回条数少于请求条数时逐条补齐，**绝不让译文与段落错位**（实测触发 3 次）
- 术语表去噪：若某大写词的小写形式也在文中出现过（`social`/`Social`），
  判定为句首大写的普通词而非专有名词。不做这步，术语表会被
  Social、Human、Full、Participants 淹没并污染每一批 prompt
- 解析层连字符规则补最强判据：去连字符后的形式若本就是文档中的词则合并。
  修正前 `Xiaohongshu` 裂成 `Xiaohongshu` / `Xiao-hongshu` / `Xiaohong-shu` 三个变体

### 已知待改进

- `DOWN` / `UP` 这类标签词，模型有时保留原文、有时按术语表译成"向下"/"向上"，
  同篇内不完全统一。属语境合理的摆动，暂不强制。
